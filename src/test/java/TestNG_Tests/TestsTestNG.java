package TestNG_Tests;

import org.testng.annotations.BeforeMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;

public class TestsTestNG {

//    @BeforeMethod
//	@BeforeMethod
//	@BeforeMethod
	@BeforeMethod
	@Test

	public void setUp() {

		/*
		 * @BeforeSuite - The @BeforeSuite method in TestNG runs before the execution of
		 * all other test methods.
		 * 
		 * @AfterSuite - The @AfterSuite method in TestNG runs after the execution of
		 * all other test methods.
		 * 
		 * @BeforeTest - The @BeforeTest method in TestNG runs before the execution of
		 * all the test methods that are inside that folder.
		 * 
		 * @AfterTest - The @AfterTest method in TestNG executes after the execution of
		 * all the test methods that are inside that folder.
		 * 
		 * @BeforeClass - The @BeforeClass method in TestNG will run before the first
		 * method invokes of the current class.
		 * 
		 * @AfterClass - The @AfterClass method in TestNG will execute after all the
		 * test methods of the current class execute.
		 * 
		 * @BeforeMethod - The @BeforeMethod method in TestNG will execute before each
		 * test method.
		 * 
		 * @AfterMethod - The @AfterMethod method in TestNG will run after each test
		 * method is executed.
		 */

	}
}
